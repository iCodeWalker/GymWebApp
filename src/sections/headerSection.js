import React from "react";

import "./headerSection.scss";

const HeaderSection = () => {
  return (
    <div className="header-container">
      <h1>Gym Name</h1>
      <p>
        This library encourages your applications to be more accessible and
        allows you to get your tests closer to using your components the way a
        user will, which allows your tests to give you more confidence that your
        application will work when a real user uses it.
      </p>
    </div>
  );
};

export default HeaderSection;
